# Reproducibility

This document summarizes the reproducibility audit for the HGSC epitype manuscript and tracks the known issues carried into the refactored monorepo. The full audit record — scorecard, per-area evidence, and environment spec — is maintained by the authors and available on request.

## Verdict: strongly reproducible

The manuscript is **strongly reproducible**. The defining result — the secretory polarization axis — reproduces **exactly**: re-running the core NMF (`atlas/03_epithelial_nmf/01_epithelial_nmf.py`) recovered SecA = Factor 3 and SecB = Factor 2 with identical assignments, gene-loading correlation **1.000** ref↔new for both factors, and 15/15 overlap in each factor's top-15 genes. The survival story is bit-reproducible: Xenium polarization Cox HRs (Fig 7A OS HR=1.45, 7B PFS HR=1.42) and TCGA-OV validation (Fig 7G OS HR=2.00 p=0.004, PFS HR=1.79 p=0.009; KM p=0.010) match the reference to printed precision.

Quantitatively:
- **Figures:** 42/67 panel rows reproduced first-pass → **54/67** after one path-typo symlink fix (the 12 unblocked panels had already matched the ledger in statistics before a render crash).
- **Backends:** **21/34 exact** (several byte-identical), **11 within-tolerance**, **1 divergent** (Fig 6J), 0 unresolved.
- **Spatial backend re-ran clean post-synthesis:** `44` autocorrelation regenerated all 7 tables (WT Lee's L p=0.001, TMA per-core significant → Fig 4E/F); GAM steps `16b`/`19d`/`19e` all exit 0.

**Integration:** the integration that produces the 1,980,703-cell atlas embedding **is included in the repository** (the official cluster scripts `atlas/01_preprocess_qc/03_preprocess_hvg.py` … `07_finalize.py`). Per-cell QC + per-sample Scrublet (< 0.3) are applied upstream in `02_concat_qc_doublets.py` (→ `atlas_concatenated_filtered.h5ad`, the chain's raw input); CellAssign annotations (`cellassign_markers_v3.csv`, 81×16) seed semi-supervised integration; **five methods are benchmarked** (scVI, scANVI, MrVI, SysVI, Harmony; scib-metrics) and **scANVI is selected** (`07_finalize --method scanvi` → `integrated_scanvi.h5ad`, obsm `X_scanvi` → `08_refilter_umap.py` → deposited `hgsc_atlas_scanvi.h5ad`). We did **not re-run** the GPU integration during validation — a validation-time choice, since it is expensive — and used the deposited integrated object as input; but the integration is fully included and scrutable, and is reproducible from raw data via the included code. The integration scripts (04–07) are now seeded from `CFG.seed` (`scvi.settings.seed` for the scVI/scANVI/MrVI/SysVI/CellAssign steps, `np.random.seed` for Harmony/benchmark/finalize); note that GPU model training is **best-effort-deterministic, not guaranteed bit-identical** across hardware, so the deposited `hgsc_atlas_scanvi.h5ad` remains the canonical reference and trust boundary (audit H1/H17). The four stochastic tools (CopyKAT, BayesPrism, LIANA, scFEA) were flanking-audited (cached outputs checked for consistency; deterministic input-prep + consumers re-run), not re-executed at their stochastic cores. The `2026_organoids/` directory is external (Fig 3 TRUST-EXISTING per author decision).

## Known issues

### Material — require author action before publication
- **Fig 6J — macrophage circularity (circularity 8/8 vs 6/8).** Published: 8/8 samples, p=0.008. Reproducible value, and the reference cache itself: **6/8, p=0.042**. Direction (circularity ↑ toward SecB-rich) holds; magnitude/significance overstated. Code migrated faithfully (`figures/figure6/08_fig_macrophage_apoptosis_prolif_morphology.R`), not changed. **Action:** correct the statistic to 6/8 p=0.042, or trace and justify the 8/8 source.
- **Fig 5F — autocrine LR shift (RESOLVED; statistically unsupported).** Full investigation on file with the authors. The generator **is** in the repo (`figures/figure5/04_atlas_seca_secb_autocrine_shift.py`) and reproduces the panel **exactly**. The issue is the threshold: Fig 5F filters on **`lrscore > 0.5`** — a raw expression-magnitude floor that passes **67.7% of all interactions** — **not** a significance/specificity test. (The manuscript's "iarscore > 0.5" is a mis-transcription of `lrscore`; `iarscore` exists nowhere in the pipeline.) At that magnitude level the asymmetry is real and the category deltas reproduce exactly (DAMP/TLR +12, ECM −10); the named DAMP/TLR pairs (HMGB1→TLR4/TLR2, S100A8/9→TLR4) clear lrscore>0.5 in SecB and are absent in SecA. **But under proper significance** (`magnitude_rank ≤ 0.05`), the shift collapses — zero TLR autocrine pairs are significant in either pole, and the only robust ECM autocrine signal (collagen→CD44/CD93/ITGA5) is shared by both. **Action (author):** either reframe as a non-inferential expression-magnitude observation (and fix iarscore→lrscore), or soften/drop the "DAMP replaces ECM autocrine signaling" mechanistic narrative. The analysis is **retained** in the refactor as-is.

### Clarifications / corrections
- **SF7 caption (3735 → 375).** Caption states "n = 3735 clones across 248 samples"; the actual informative-clone count is **375** (factor-of-10 digit-insertion typo). All other SF7/SF4C numbers verified exact. **Action:** correct "3735" → "375".
- **BayesPrism multivariate covariate set.** The exported "full multivariate" model defaults to including platinum sensitivity, which dominates and renders polarization non-significant. The manuscript-stated model (epithelial fraction + stage + age), re-fit here, **retains significance** (OS p=0.039, PFS p=0.041). **Action:** clarify in Methods that the multivariate adjustment is epi fraction + stage + age (not platinum sensitivity).

### Reproducibility-hardening (applied / documented in the refactor)
- **Cohort drift handling.** The live cache had 2 extra FTE whole-tissue samples (`FT1-1`, `EAOC-1-FTE`) added post-preprint, inflating FT/WT/TMA n. The refactor **pins the published cohort** via `CFG$cohort$whole_tissue` (8 WT; FTE whole-tissue excluded, 15 FTE TMA cores retained), resolving 4A/4B/4E n inflation and the SF11 assertion. Cohort-drift details are on file with the authors.
- **Non-determinism now seeded.** `set.seed(CFG$seed)` added to Xenium cell-labeling `spatial/03_annotation_polarization/04_reclassification_polarization.R` and `05_clean_split_rctd.R` (source of TMA n/p and per-sample GAM consistency-count drift) and to `atlas/07_deconvolution_survival/08_consensusov_score.R` (RF flips 2 samples IMR↔MES). Python steps seed from config `SEED`.
- **HR reciprocal convention.** Scripts compute polarization as SecA-high (HR<1, protective); the manuscript reports the SecB-high reciprocal. 5-yr-clipped HRs invert exactly to the published values. The convention is documented so the mapping is explicit (`figures/figure7/`, `atlas/07_deconvolution_survival/05_signature_survival.py`).
- Cosmetic: scFEA flux-label-map bug fixed (`atlas/04_functional/01_epitype_functional_characterization.py`); `torch` dependency needed only for the scFEA step; "Transitioning" → "Intermediate" naming standardized across the polarization display vocabulary.

### Blind re-audit follow-ups (applied)

A second, independent code audit surfaced a layer of producer→consumer wiring gaps and one mislabeling bug downstream of the integration trust boundary. Fixes applied:

- **Clean-room regeneration: two missing producers migrated into the repo.**
  - `spatial/02_qc/05b_core_qc.R` (from the source `core_qc.Rmd`) writes `07_core_qc/{core_qc_summary,patient_core_status,patient_concordance}.csv` — the core-exclusion list `06_filter_tma.R` consumes to build `sfe_tma_filtered`. Deterministic; the **deposited copy remains authoritative** for the published cohort, and this script reproduces it.
  - `spatial/08_clinical_survival/06b_spatial_distance.R` (from `29d_spatial_distance.R`) writes `29_macrophage_niche_survival/spatial_distance_{per_sample_wt,per_patient_tma}.csv`, read by `07_figures.R` for **Fig 6 panel 29D** (previously `07` hard-errored on a clean `output_root`).
- **Proportion-statistics prefix fix.** `atlas/02_annotation/12b_proportion_statistics.py` read `12_`-prefixed count tables that no script writes (the producer emits `12b_`); the reader now uses `12b_`, so the Fig 1B–E / Fig 2 composition-statistics report regenerates instead of returning empty and crashing.
- **Single source of truth for neighbourhood names (Fig 6 niche backend).** `spatial/04_neighborhood/02` no longer redefines a divergent integer-keyed `nb_names` that conflicted with `00_setup.R`; all stages use the one canonical, composition-grounded map (verified against `neighborhood_composition.csv`). The four immune-niche scripts (`05–08`) filtered on names (`"Immune niche"`, `"SecB-enriched epithelium"`) that matched **no** cluster and selected zero cells; they now use `"Immune-rich niche"` (nb_4) and `"Intermediate-SecB epithelium"` (nb_5) with a fail-loud `stopifnot`. **Residual (author action):** the integer→biology map assumes the canonical k-means numbering — a fresh re-run should verify identities against `neighborhood_composition_k10.csv`; and the secondary `epi_nbs`/`epi_nb_labels` gradient maps in `05_macrophage_niche_analysis.R` still carry non-canonical labels to reconcile.
- **Fig 4A/B cohort pin extended.** `figures/figure4/01` now pins the whole-tissue arm to `CFG.cohort.whole_tissue` so the 2 FTE whole tissues no longer leak in via the 06f cache (10→8 WT bars/donut), with an assertion.
- **Environment completeness.** `environment.yml` now declares `pyyaml` (config loader), `scvi-tools` + `scib-metrics` (integration chain / benchmark), and `adjustText` (Fig 2C volcano) — all imported but previously undeclared.

**HIGH-severity batch (also applied):**
- **Fig 7 survival classification producer migrated.** `atlas/03_epithelial_nmf/01b_gene_classification.py` derives `11e_gene_classification.csv` (SecA/SecB program split + loadings) from `11d_nmf_loadings.csv`; `05/06_*survival.py` now read it from `output_root` (deposited fallback) instead of a bare `data_root` literal with no producer.
- **Integration chain seeded.** `atlas/01` steps 04–07 now seed from `CFG.seed` (`scvi.settings.seed` for scVI/scANVI/MrVI/SysVI/CellAssign; `np.random.seed` for Harmony/benchmark/finalize). Best-effort: GPU model training is not bit-identical across hardware, so the deposited integrated object remains the trust boundary (docs updated).
- **Unfiltered TMA reads fixed.** `spatial/04_neighborhood/09,10` now load `sfe_tma_filtered` (not unfiltered `sfe_tma`), matching the cohort the neighborhood assignments are keyed to.
- **anndata 0.12.2 backed-sparse reads removed.** The three NMF-input loaders (`atlas/03/01,03`, `tables/01`) now read in-memory instead of `backed="r"` (the 0.12.2 backed-sparse `.X`/`layers` path is broken).
- **`organoids_root` de-duplicated.** Removed the `objects.organoids_root` key; all 6 Fig 3 consumers resolve it via `path`/`cfg_path("organoids_root", ...)`, so an absolute `ORGANOIDS_ROOT` override works in R and Python.
- **12d label collision fixed.** The level-2 "Transitioning epithelial cell" cluster is displayed as "Transitioning" (not "Intermediate"), no longer colliding with the NMF polarization Intermediate.
- **SF9 panel count.** The reproduction script no longer emits a 6th "Metastatic site" panel — it matches the published 5-panel SF9A–E.
- **SFE writes no longer mutate `data_root`.** `save_sfe()` writes regenerated SFEs to `output_root/sfe`; `load_sfe()` prefers that and falls back to the deposited `data_root` copy — downstream re-runs never overwrite/unlink the read-only deposited inputs. (The from-raw build/QC writers `01/02_build`, `02_qc/04` still write into `data_root` and are a flagged follow-up — they regenerate the deposited bundle itself.)
- **07→08 integration chain continuity.** `08_refilter_umap.py` prefers `07_finalize`'s `output_root` integration output, falling back to the deposited object.
- **07_figures.R titles computed from data.** Panel 29B/29D p-values, %-positive/closer, and patient counts are now computed from the loaded tables (were hardcoded and inconsistent with the documented 97-patient TMA); the wrong "111 patients" 29A label was removed.

**Remaining (flagged for author):** the niche integer→biology map assumes the canonical k-means numbering (verify against `neighborhood_composition_k10.csv` on a fresh re-run; reconcile the secondary `epi_nbs` labels in `05_macrophage_niche_analysis.R`); the from-raw SFE build/QC writers still target `data_root`; and all fixes are static-verified (syntax + logic + cross-checked against deposited caches) but NOT executed — the two migrated producers (`05b_core_qc`, `06b_spatial_distance`) and `01b_gene_classification` should be run once and diffed against the deposited caches to confirm they reproduce.

**Runtime verification (executed against deposited inputs):**
- `01b_gene_classification.py` — **reproduces the deposited `11e_gene_classification.csv` exactly**: identical 3007 genes, 100% per-gene class agreement, identical 177 SecA / 177 SecB / 124 Shared / 2529 Background split, zero floating-point difference in loadings. The Fig 7 survival producer port is confirmed.
- `05b_core_qc.R` — runs on the deposited `sfe_tma` (671k cells) and reproduces the deposited `core_qc_summary.csv` to **97%** (100% on flag_small / flag_low_epi / flag_low_confidence; zero diff on `median_singler_score`). Two borderline cores (107, 126) differ on the Mahalanobis composition-outlier flag → 14 vs 16 excluded cores. **Root cause is input drift, not the port:** the current deposited `sfe_tma` carries empty-string (`""`) `cell_label` values for a sizable fraction of cells in those two cores (≈31% / ≈26%), and both cores sit at the composition-outlier threshold (core 107 maha = 33.58 vs cutoff 33.4). The deposited summary is therefore a **snapshot of an earlier object state** that no longer re-derives exactly from the current object. **Author decision:** regenerate `core_qc_summary.csv` from the current `sfe_tma` (run `05b`) and adopt it as the canonical, self-consistent cohort, or pin the exact `sfe_tma` state behind the published exclusions. (`06b_spatial_distance` not yet executed — same faithful-port pattern; `RANN`→`FNN` is exact-NN-equivalent.)

### Batch 2 — user-triaged follow-ups (cohort resolved + multi-agent fixes)

- **TMA cohort RESOLVED — the paper uses the 16-excluded-core set.** The published 97-patient TMA cohort comes from the FROZEN deposited `07_core_qc/core_qc_summary.csv` (16 cores excluded): patient 5223 is absent from the 97 only if core 126 is excluded, and 14-exclude would yield 98. `spatial/02_qc/06_filter_tma.R` now reads the deposited frozen table preferentially (falling back to a locally regenerated copy with a warning). `05b_core_qc.R` remains as the scrutable producer, but on the current (drifted) `sfe_tma` it reproduces only 14 excludes — so it is documented as a regeneration/scrutiny tool, NOT the canonical published cohort. (The drift: the current `sfe_tma` carries empty `""` cell_labels in cores 107/126, which sit at the composition-outlier threshold.)
- **Manuscript-text corrections identified** (Fig 6J 8/8→6/8; Fig 5F autocrine reframe + iarscore→lrscore; SF7 caption 3735→375; BayesPrism multivariate = epi fraction + stage + age, drop platinum; Fig 6 niche FTE-pooling note; remove the Gavish/metaprogram NMF Methods passage — confirmed not shown in any figure and has no code).
- **METHODS.md**: removed the Gavish/metaprogram consensus-NMF passage (no supporting figure or code); rewrote the integration paragraph to state atlas/01 03–07 are the authoritative cluster originals (removed the self-contradictory "inferred hyperparameters" / "CONFIRM" language); 3,735→375 clones.
- **Code fixes (13 agents, disjoint files, all syntax-verified):** dropped platinum from the multivariate Cox (04/05 survival); KM colors keyed by group name; CopyKAT reference subsample made deterministic (sorted before sampling); spatial-03 atlas-UCell input path corrected (un-breaks the polarization chain); 02 adaptive-secretory made idempotent; vascular_proximity routed through `cfg_path`; NMF SecB=Factor_2 safety (01 persists the factor map; 02/04 assert it) and 02 no longer mutates the deposited `atlas_final.h5ad`; Fig 3B/D/E atlas reference line restored via the rename; Fig 6A/6D Intermediate cells now colored (rename) + fig6/05 crash-guarded; SF3A reworked to the treatment-status UMAP; SF11 reworked to the 3-cohort distribution; A4 niche identity table added (`02b_neighborhood_identity_table.R`) + the mislabeled `epi_nb_labels` corrected; decoupler pinned 2.x + survRM2/SPLIT added to renv; figure/morphometry README cache-contract drift corrected.

**Follow-ups from this batch:**
- **`atlas/06_cellcomm/01_cellcomm_nmf.py` — FIXED.** The positional `.values` assignment of `celltype_nmf` was replaced with a barcode (index) `reindex` join + a fail-loud assertion (the NMF mapping covers all 1.98M atlas barcodes — the same set as `atlas_final.obs` — so the join is exact and can no longer silently mis-wire the LIANA cell types).
- **TCGA multivariate (A2) — RESOLVED by empirical re-fit.** Fig 7G is the atlas-side deconvolution survival (`atlas/07` → `22d`). The reported significant multivariate uses **epithelial fraction + stage + age (no platinum)**: re-fitting `22d_signature_scores.csv` reproduces the deposited univariate exactly (OS p=0.0047) and yields **OS p=0.039 / PFS p=0.041** for that model (significant = the manuscript). Adding `platinum_coded` (the deposited code's erroneous "full-multivariate" default) makes polarization NON-significant (OS p=0.28, PFS p=0.75) — confirming G2's drop-platinum edit. The separate `spatial/09_external_validation/01_tcga_external_validation.R` (age+stage, UCell/CIBERSORTx) is a DISTINCT external validation, not Fig 7G — no contradiction.
- **Cohort-inlining sweep — DONE.** All 23 inline whole-tissue enumerations across the spatial backend were centralized: 21 scripts (which source `00_setup`) now use `sfe_names_wt`, and the 2 `05_gradients_gams/0{2,3}_gene_polarization_gams.R` scripts (which source config directly, not `00_setup`) use `paste0("sfe_", CFG$cohort$whole_tissue)`. Confirmed **value-identical** to the inlined 8 (same names, same order) and all 23 files re-parse. Single-sample ROI scripts, the SF12–14 colour palette, and the per-sample `01/02_build_sfe` writers were left untouched (they name individual samples, not the cohort). The cohort now lives only in `config.yml` (single source of truth).
- **SPLIT** renv lockfile `RemoteSha` is a placeholder (needs the GitHub commit SHA; no network to resolve it). **METHODS line ~212** states monoclonal "140 (58%)"; 140/248 = 56% (minor — for the corrections list).
- **`06b_spatial_distance` re-run (item E) — DONE; deterministic.** Re-running reproduces the published FINDING (macrophages closer to top-hypoxia anchors than lymphocytes: WT 100% mac-closer, p=0.0039 — bit-identical to the deposited cache; TMA ~82%, p≈1e-10). Exact magnitudes differ from the deposited cache (WT median Δ −13.7 vs −18.6 µm; TMA 90 vs 88 patients) — the SAME upstream object drift as core-QC (current SFE 06f labels + TMA patient set differ from the frozen snapshot), NOT nondeterminism. So the conclusion reproduces; bit-identical cached numbers do not.
- **Paper-fidelity pinning (Option 1, applied).** Published-panel renderers that read drift-prone backend caches now read the FROZEN deposited copies: `06_filter_tma.R` → frozen core-QC table (the 97-patient cohort), `07_figures.R` → frozen `29_macrophage_niche_survival` caches (niche-survival panels incl. 29D n=88). `figure6/03,04` read a figure-internal cache (`gradient_metrics.csv`) that `figure6/01` computes from stable inputs (frozen 06f labels + stored hypoxia), so they reproduce. `SF12–14` (vascular GAMs) inherently recompute — the deposited `vascular_distance_all_cells.csv` lacks the `cell_id` key the keyed merge needs — so they reflect re-run values; flagged. **Root-cause + full-from-raw path documented for the manuscript** (in-place SFE mutation + unseeded RCTD/06f labeling → published spatial numbers live in the frozen caches; full from-raw reproducibility requires a seeded backend re-run + regenerating/updating all spatial figures — a re-analysis, not a reproduction).

## External / side-quest validations (placeholder figures)

These are exploratory analyses on external public cohorts, slated as placeholder panels not yet assigned a manuscript number (see `figures/README.md` → "Pending / placeholder panels"). They live in source modules under `…/2026_final_xenium_analysis/davids side quests/` and are wired into the refactor as figure dirs.

- **CCLE / DepMap SMAC-mimetic side-quest (placeholder; panel not yet assigned).** Data = DepMap 24Q4 Public (Figshare: `Model.csv`, `OmicsExpressionProteinCodingGenesTPMLogp1.csv`, `CRISPRGeneEffect.csv`/Chronos) + PRISM Repurposing Harmonized Secondary Screen 25Q2 AUC (DepMap portal). **Fully reproducible locally** (R 4.5.2 + UCell/data.table/clusterProfiler, ~5 min on a MacBook; no GPU/cluster step). Per-line SecA/SecB scores via z-mean of log2(TPM+1) (primary; UCell secondary, the two agree ρ=0.70), polarization = z(SecB) − z(SecA). Reproduced numbers: SMAC mimetics are negatively correlated with polarization (SecB-high lines more sensitive) — **LCL-161 ρ=−1.00, exact p=0.017 (n=5); GDC-0152 ρ=−0.71, p=0.088 (n=7); birinapant ρ=−0.50** — while chemo controls (carboplatin, doxorubicin) are not; single-gene **BIRC3 Chronos is null/opposite (ρ=+0.22)**, upstream **TRAF2 trends (ρ=−0.45, p=0.08)**. **Caveats (read as hypothesis-generating, P2-level):** very small n (16 lines with Chronos, only 5–7 with PRISM AUC per compound; five of the most SecB-polarized lines lack Chronos); 2D culture compresses the SecB program (most HGSC lines collapse toward SecA), diluting any SecB-specific dependency; PRISM AUC is a within-batch *relative* viability readout; single-gene CRISPR cannot model IAP-family redundancy (cIAP1/cIAP2/XIAP), which pan-IAP SMAC mimetics bypass — the most likely reason the pharmacological signal appears while the BIRC3 genetic readout does not. Source module + full report: `…/davids side quests/ccle_depmap/`.
- **ICON7 / GSE140082 bevacizumab side-quest (placeholder; panel not yet assigned).** Per-arm Cox on baseline SecB–SecA polarization: prognostic under chemo, abolished by bevacizumab (slope reversal). Reproducible locally; source module + report: `…/davids side quests/ICON7/` (also `figures/figure_icon7_bevacizumab/README.md`).

## Environment
- **Python (`epitype-py`):** 3.12.3, scanpy 1.11.4, anndata 0.12.2, numpy 1.26.4, numba 0.59 (+ lifelines, decoupler, liana).
- **R 4.5.2 stack:** SingleR 2.10.0, UCell 2.12.0, mgcv 1.9-4, spdep 1.4-2, spatstat 3.6-1, survival 3.8-6, BayesPrism 2.2.3, consensusOV 1.30.0, copykat 1.1.0, SpatialFeatureExperiment 1.10.1, Voyager 1.10.0 (see `docs/r_key_package_versions.csv`).

# CLAUDE.md

The agent/contributor guide for this repository lives in **[`AGENTS.md`](AGENTS.md)** — read it
first. It covers the repository layout, the path/config conventions (resolve everything through
`config/`, never hardcode), data handling (`DATA_ROOT` / `OUTPUT_ROOT`), the integration trust
boundary, and how to reproduce a single manuscript panel.
